{
  "name": "@openparachute/claw-server",
  "version": "0.0.1",
  "private": true,
  "description": "Paraclaw orchestration server. Reads claws/* from vault, exposes a REST surface for the UI, manages runtime processes.",
  "license": "AGPL-3.0",
  "type": "module",
  "module": "src/server.ts",
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "bun --watch src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
